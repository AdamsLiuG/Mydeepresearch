services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: helloagents-deepresearch-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    volumes:
      - ./backend/notes:/app/notes
    restart: unless-stopped
